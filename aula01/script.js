$(document).ready(function() {  // Garante que o código será executado somente quando o DOM estiver pronto
    $('#btn').click(function() {  // Quando o botão for clicado
        $('#msg').text('Você clicou no botão!');  // Altera o texto do parágrafo com id 'msg'
    });
});

